name: Prettier

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  prettier:
    name: Prettier
    uses: ./.github/workflows/nodejs-pnpm.yml
    with:
      build: false
      test: false
      os: '["ubuntu-latest"]'
